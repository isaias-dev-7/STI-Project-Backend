const resources = '/resources/';

export enum uploadsPathEnum {
    SUBJECT_IMAGE = '/subjectImages/',
    DOC = `${resources}documents/`,
    AUD = `${resources}aud/`,
    IMAGE = `${resources}images/`,
    VIDEO = `${resources}video/`
}